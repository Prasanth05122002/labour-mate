server:
  port: 8083
spring:
  application:
    name: job
  datasource:
    url: jdbc:mysql://localhost:3306/labourmate
    username: root
    password: Good$123
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true